import 'package:flutter/material.dart';
import 'package:controles/src/pages/controles.dart';

class MyApp extends StatelessWidget {
  Widget build(context) {
    return MaterialApp(
        home: Center(
      child: ControlesPage(),
    ));
  }
}
