import 'package:flutter/material.dart';
import 'package:first_exam_flutter/src/pages/controles.dart';

class MyApp extends StatelessWidget {
  Widget build(context) {
    return MaterialApp(
        home: Center(
      child: ControlesPage(),
    ));
  }
}
